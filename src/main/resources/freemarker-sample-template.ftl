# Bounded Contexts in ${fileName}

The model contains the following Bounded Contexts:
<#list boundedContexts as bc>
 * ${bc.name}
</#list>

# Custom variables

You can also use custom model objects and attributes for rendering. An example:
${customText}
